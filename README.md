Comando para descargar el script:

```powershell
iwr -Uri https://raw.githubusercontent.com/sehorzero/frida/refs/heads/main/Frida.ps1 -OutFile .\
```

Quizá sea más cómodo modificar el parámetro `-OutFile .\` a una ruta especifica, por ejemplo `-OutFile C:\Users\$env:UserName\Desktop`.
